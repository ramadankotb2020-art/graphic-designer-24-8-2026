# RK Design — سجلّ التعديلات المنفّذة (FX Upgrade)

تم تطبيق كل المراحل على نسخة كاملة شغّالة الآن في الـ preview.
**مبدأ التفيذ:** كل الحركة بـ `transform`/`opacity` فقط (GPU)، مع احترام كامل لـ `prefers-reduced-motion` وتعطيل تلقائي على شاشات اللمس.

---

## ✅ المرحلة 1 — الأداء والإصلاحات
- **حوّلت صور الهيرو والصفحة الرئيسية لـ WebP** مع تصغير الأبعاد:
  - `hero-slide-1`: 1.5M → **149K** · `hero-slide-3`: 2.8M → **240K** · `about-portrait`: 2.1M → **96K**
  - متوسط التخفيض **~90%** لحجم صور الـ LCP.
- **أضفت `preload` + `fetchpriority="high"`** لأول صورة هيرو = ظهور فوري.
- **أضفت `width`/`height`** لكل صور الصفحة = منع layout shift (CLS).
- **حذفت صور `*-PLACEHOLDER.jpg`** المكررة.
- **أنشأت `js/page-bg.js`** (كان مفقودًا ويسبّب 404) — خلفية ambient خفيفة بتتحمّل بس لغير Data-Saver.
- حدّثت `slides.js` و`home.js` لاستخدام نسخ WebP.

## ✅ المرحلة 2 — إبهار الدخول
- **Preloader سينمائي (Curtain Wipe):** الشاشة بتتنصّف نصّين بينفتحوا + المارك "RK" بيختفي — `css/rk-fx.css`.
- **Split-Text للهيرو:** عنوان الهيرو بيتقسّم كلمات، وكل كلمة تطلع من قناع بتأخير متدرّج (stagger) — أكتر إفكت بيعمل wow، مع `MutationObserver` يعيد التقسيم عند كل تغيير شريحة.
- **Aurora:** طبقة ضوء ذهبية مخروطية بتدور ببطء بـ `mix-blend-mode: screen` فوق الهيرو = مود/إيموشن.

## ✅ المرحلة 3 — التفاعلات (desktop)
- **Cursor مخصص:** حلقة ذهبية بتتبع الماوس بـ lerp سلس + نقطة، وتكبر فوق العناصر التفاعلية، وتصغر عند الضغط.
- **Magnetic Buttons:** زراير الـ CTA بتميل ناحية الماوس.
- **Image Tilt + Sheen:** كروت الخدمات بتميل 3D خفيف + لمعة ذهبية بتمرّ عليها عند الـ hover.

## ✅ المرحلة 4 — Scroll & Motion
- **Marquee:** شريط كلمات مفتاحية متحرك لا نهائي بعد الهيرو مباشرة (طاقة/حركة داومة).
- **Parallax خفيف:** صورة "عن رمضان" والكارت العائم (+150) بيتحركوا أبطأ من السكرول = عمق سينمائي.

## 📂 ملفات جديدة
- `css/rk-fx.css` — كل تنسيقات الإفكتات
- `js/rk-fx.js` — محرّك الإفكتات
- `js/page-bg.js` — إصلاح الخلفية المفقودة

## ⏭️ لسه ممكن نعمله (لو حبيت)
- فصل داتا `room-designer.html` (1.8MB) لملف JS خارجي.
- Showreel فيديو قصير لأعمال الجرافيك المتحركة + Kinetic Typography في التاج.

---

## 🔁 الجولة التانية — تعميم + أداء شامل

### ✅ تطبيق الـ FX على كل الصفحات
- ربطت `css/rk-fx.css` + `js/rk-fx.js` في: **about, graphic-design, interior-design, exterior-design, contact, project**.
- عمّمت **Split-Text** بحيث يشتغل على عناوين `.page-hero .headline-xl` في صفحات الأقسام (مش بس الرئيسية) → كل صفحة بتبدأ بعنوان بيطلع كلمة كلمة بعد ما الستارة تفتح.
- النتيجة: **cursor + أزرار مغناطيسية + curtain preloader** بقت متسقة على الموقع كله.

### ✅ تحويل كل صور المشاريع لـ WebP
- حوّلت **كل** صور `images/projects-by-name/` لـ WebP (resize 1600px, جودة 78) وحذفت النسخ الأصلية.
- **حجم صور المشاريع: ~599MB → 5.4MB** (−99%)، وإجمالي `images/`: **625MB → 14MB**.
- حدّثت كل الإشارات في `projects-data.js` و`projects.js` من `.png/.jpg` → `.webp`.

### ✅ معالجة الصور المكسورة (بديل أنيق)
- أضفت معالج `error` (capture-phase) يحوّل أي صورة مشروع ناقصة لـ **placeholder أنيق** بخلفية ذهبية + مونوجرام "RK" + اسم المشروع — بدل أيقونة مكسورة.
- مفيد جدًا لو فيه مشروع لسه مضافش صوره (بيظهر إطار بديل فخم).

### ⚠️ ملاحظة عن بيئة الديمو
- snapshot البيئة محدود بـ 128MB، فقصّ ~229 صورة مشروع من الديمو (في الريبو الحقيقي بتاعك كل الصور موجودة). الصور الناقصة بتظهر بالـ placeholder الأنيق، ومجرد ما ترفّع التحديثات على ريبوك هتظهر كلها طبيعية.

---

## 🤩 الجولة التالتة — باقة الإبهار القصوى + أداء room-designer

### ✅ إفكتات إبهار جديدة (كلها GPU، تحترم reduced-motion)
- **Spotlight على الهيرو:** بقعة ضوء ذهبية بتتبع الماوس (`mix-blend-mode: screen`).
- **لمعان ذهبي متحرك (Shimmer):** على كل كلمات `.gold` وعنوان الهيرو — تدرّج ذهبي بيتحرّك.
- **طبقة Grain سينمائية:** حبيبات فيلم خفيفة بتتحرك = إحساس فاخر (SVG noise، opacity 0.045).
- **Clip-Path Reveal:** كروت الخدمات وصورة "عن رمضان" بتكشف من تحت لفوق بـ stagger.
- **قسم Showreel سينمائي:** قسم كامل بعد الـ marquee فيه خلفية + زر تشغيل نابض، بيفتح **Modal فيديو** (videos/2.mp4).
- **Kinetic Typography:** في الـ Showreel كلمة بتتبدّل أوتوماتيك (الديكور الداخلي ← الهوية البصرية ← الواجهات ← الرندر 3D) بحركة slide + لمعان.
- **لون تحديد ذهبي** (`::selection`).

### ✅ فصل داتا room-designer (1.8MB!)
- طلعت الـ `__RK_ASSETS_INDEX__` (1.46MB base64) + `__RK_PORTFOLIO__` لملف خارجي `js/rk-assets-index.js`.
- **room-designer.html: 1.8MB → 367KB** (−80%) — الصفحة بقت تفتح بسرعة، والداتا بتيجي async وقابلة للـ cache.

### 📊 النتيجة الإجمالية للمشروع
- **حجم الصور: 625MB → 14MB** · **room-designer: 1.8MB → 367KB** · إجمالي المشروع **~26MB**.
- كل الصفحات بتحمل `200` بدون أي 404.

## ⏭️ لسه ممكن نعمله (لو حبيت)
- استخراج الـ module script في room-designer لملف خارجي كمان (caching إضافي).
- ربط Showreel فيديو مونتاج حقيقي لأعمالك لمّا يجهز.
- إضافة المزيد من الـ marquee/parallax على صفحات الأقسام لو حبيت نفس إبهار الرئيسية.
