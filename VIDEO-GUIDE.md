# 📹 دليل الفيديوهات — RK Design

دليل كامل لإضافة وتغيير الفيديوهات في الموقع. **البنية التحتية كلها جاهزة** — إنت بس بتضيف ملفات الفيديو والباقي أوتوماتيك.

---

## 1️⃣ إضافة فيديو لأي مشروع (الأهم) ⭐

دي الطريقة اللي تخلي **كل مشروع يبقى ليه فيديو** على الكارت.

1. اعمل فيديو للمشروع (أي مدة، يفضل قصير 5–20 ثانية، loop).
2. سمّيه **`cover.mp4`** (الاسم ده مهم — ده اللي بيظهر على الكارت).
3. حطه جوّه فولدر المشروع:
   ```
   images/projects-by-name/<القسم>/<اسم المشروع>/cover.mp4
   ```
   مثال:
   ```
   images/projects-by-name/interior/شقة معاصرة — جسر السويس/cover.mp4
   ```
4. دوس **`update-site.bat`** → الفيديو:
   - بيتهضم ويظهر على الكارت أوتوماتيك ✅
   - بيتضغط عشان مايقلّش الموقع ✅

### إزاي بيظهر الفيديو على الكارت؟
- 🖥️ **ديسكتوب**: العميل يقف بالماوس على الكارت → الفيديو يشتغل.
- 📱 **موبايل**: العميل يدوس شارة **"▶ فيديو"** → الفيديو يشتغل.
- 🖼️ لو مفيش فيديو → الصورة العادية.

> 💡 **القاعدة الذهبية**: فيديو اسمه `cover.mp4` جوّه فولدر المشروع = فيديو على الكارت أوتوماتيك.

### 🎬 مولّد فيديوهات تلقائي (للمشاريع اللي ملهاش فيديو)
`update-site.bat` بقى **بيولّد فيديو من صور المشروع** أوتوماتيك (Ken Burns + انتقالات) لأي مشروع ماعندوش `cover.mp4`. يعني:
- لو حطيت `cover.mp4` → الموقع بيستخدم فيديو**ك الحقيقي**.
- لو ماحطيتش → الموقع **بيعمل فيديو تلقائي من صور المشروع** → **كل مشروع يبقى ليه فيديو**.

---

## 2️⃣ البانرين بتوع الصفحة الرئيسية (الـ parallax)

البانرين دول (واحد للديكور + واحد للجرافيك) بيجيبوا الفيديو بتاعهم **تلقائيًا من فيديوهات مشاريعك**:
- **بانر الديكور** (فوق) → بيدور على فيديوهات مشاريع التصميم الداخلي.
- **بانر الجرافيك** (تحت) → بيدور على فيديوهات مشاريع الجرافيك.
- كل ساعة بيغيّر الفيديو لوحده.

يعني **لمّا تضيف فيديوهات للمشاريع، البانرين هياخدوها أوتوماتيك** — مفيش حاجة تعدّلها. ✅

### لو عايز تحط فيديو ثابت معيّن للبانر (اختياري)
عدّل `js/parallax.js` (سطر ~23):
```js
const VIDEOS = {
  interior: 'videos/hero-bg.mp4',   // fallback للبانر الأول
  graphic:  'videos/2.mp4'          // fallback للبانر التاني
};
```
(دي بتستخدم بس لو مفيش فيديوهات مشاريع لسه)

---

## 3️⃣ فيديوهات الغلاف بتاع الصفحة الرئيسية (الـ slider)

دي الصور/الفيديوهات اللي بتتبدّل في أعلى الصفحة الرئيسية.

**الملف**: `js/slides.js` — كل شريحة فيها:
```js
// صورة
{ type:'image', image:'images/homepage/الصورة.webp', tag:'...', title:'...', sub:'...', btn_text:'...', btn_href:'...', dur:6 }

// فيديو
{ type:'video', src:'videos/الفيديو.mp4', tag:'...', title:'...', sub:'...', btn_text:'...', btn_href:'...' }
```
غيّر المسارات/النصوص، احفظ، اعمل refresh.

---

## 4️⃣ سير العمل الكامل (خلاصة)

| عايز تعمل إيه | اعمل إيه |
|--------------|---------|
| فيديو لمشروع | حط `cover.mp4` في فولدر المشروع → `update-site.bat` |
| تغيّر فيديو غلاف الصفحة | عدّل `js/slides.js` |
| تغيّر فيديو بانر (fallback) | عدّل `VIDEOS` في `js/parallax.js` |

---

## نصائح للفيديو 🎬
- ⏱️ **قصير** (5–20 ثانية) وأفضل لو **loop** (يتكرر بسلاسة).
- 📐 أفقى (landscape).
- 🗜️ `update-site.bat` بيدغطه أوتوماتيك — متقلقش من الحجم.
- 🔇 الصوت مش لازم (الموقع بيشتغلها mute).
- ✏️ اسم الملف **`cover.mp4`** بالظبط عشان يظهر على الكارت.

---

## لو فيه مشكلة
- الفيديو مش بيظهر → تأكد اسمه `cover.mp4` (مش Cover.mp4 ولا video.mp4).
- الفيديو تقيل → شغّل `update-site.bat` هيدغطه.
- عايز أشوف أنهي مشاريع متنفعش بيها فيديو → افتح الكونسول بعد `update-site.bat` (بيقولك).

🎉 **بكده كل حاجة جاهزة — اعمل فيديوهاتك وحطها، والموقع هياخدها لوحده.**
