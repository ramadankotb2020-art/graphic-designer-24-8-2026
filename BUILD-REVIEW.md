# مراجعة شاملة — نظام المشاريع + خط الـ Build

## 🔬 التشخيص: ليه في مشاريع مختفية؟

| السبب | التفاصيل | الحل |
|-------|----------|------|
| **الديمو** | 12 فولدر مشروع (تصميم داخلي) **تمسحت كل ملفاتها** (صور+فيديو) بسبب حد تخزين البيئة (128MB) → الـ cover بيكسر → الكارت بيطلع فاضي/بديل. **دي مش مشكلة كود.** | عندك محليًا الفولدر كامل، فالمشروع هما موجودين. |
| **الداتا قديمة** | `projects-data.js` فيه **54 مشروع**، بس الفولدرات الفعلية **53** (graphic: 24 في الداتا لكن 23 فولدر). يعني الداتا مش متزامنة مع الفولدر. | **أعد تشغيل `update-site.bat`** = يتزامن من جديد. |
| **فجوة في السكريبت** | أي فولدر فيه **فيديو بس (من غير صور)** السكريبت بيتجاهله (`if (!images.length) return null`). لو عندك مشاريع بالفيديو بس → مش هتظهر. | نطوّر السكريبت (تحت). |

---

## 🏗️ مراجعة خط الـ Build (`update-site.bat` ← `scripts/build-projects.js`)

### ✅ كويس
- بيمسح `images/projects-by-name/<قسم>/<مشروع>/`، **أول صورة أبجديًا = الغلاف**، وبيولّد `js/projects-data.js`.
- `info.txt` اختياري لتفاصيل المشروع (عنوان، تصنيف، وصف، **مميز: نعم** للأعمال المميزة).
- `update-site.bat` بيتحقق إن Node متثبت وبي logged الأخطاء في `update-log.txt`.
- حسب تعليقات السكريبت، بيعمل build تلقائي على **Cloudflare** مع كل push.

### ⚠️ مشكلات
1. **فولدرات الفيديو بس بتتتجاهل** — لو مشروع مايلهوش صورة (فيديو بس) مش هيظهر نهائي.
2. **الداتا بتشير للصور بالامتداد الأصلي** (`.png`/`.jpg`) = **600MB تقيل**. مفيش تحسين (webp/resize).
3. `DISCIPLINE_FOLDERS` معرّفة بالأسماء العربية (`تصميم-داخلي`) بس الفولدرات اسمها **إنجليزي** (`interior`). الـ fallback بيحلها، بس الـ map نفسها dead code.
4. لو أعدت تشغيل `update-site.bat`، **هيرجّع المسارات لـ .png/.jpg** ويمسح تعديلاتي (تعديلات الـ webp دي للديمو بس).

---

## 🐌 فولدر الـ 600MB — أداء
ده أكبر سبب لثقل الموقع. الحل:
- **حوّل كل الصور لـ WebP + صغّر الأبعاد** (1600px، جودة 78) → الحجم بينزل **80–90%** (من 600MB لـ ~50MB).
- ممكن يتعمل بطريقتين:
  - **(أ)** سكريبت تحسين منفصل تشغّله مرة على الفولدر.
  - **(ب)** نطوّر `build-projects.js` نفسه يعمل التحويل أوتوماتيك أثناء الـ build.

---

## ✅ التوصيات (مرتّبة بالأهمية)

1. **أعد تشغيل `update-site.bat` محليًا** → يولّد `projects-data.js` متزامن مع كل المشاريع الحالية.
2. **طوّر `build-projects.js`**:
   - معالجة فولدرات الفيديو (لو مفيش صورة، استخدم أول فيديو كـ cover أو اعرض المشروع بفيديو).
   - إضافة **تحسين الصور لـ WebP** (أو توليد مسارات webp لو الفولدر متحوّل).
3. **ارفع التعديلات على الريبو** (FX، إصلاح كروت المشاريع، البانرات فيديو، إلخ) → Cloudflare يبنيها وتظهر على الموقع الحقيقي.
4. تأكد إن **كل فولدر مشروع فيه صورة واحدة على الأقل** (مش فيديو بس) عشان يظهر.

---

## 🤝 أقدر أعمله دلوقتي
- أطوّر `scripts/build-projects.js` عشان: (أ) مايتجاهلش فولدرات الفيديو، (ب) يطلع مسارات webp تلقائيًا لو الصور متحوّلة.
- أعملك **سكريبت `optimize-images`** يشغّله مرة على فولدر الـ 600MB يحوّله لـ webp خفيف.
- أجهّز **ملف التغييرات** اللي لازم ترفعها على الريبو عشان الموقع الحقيقي ياخد كل التحسينات.

قوللي تحب أبدأ بإيه. 🙌
